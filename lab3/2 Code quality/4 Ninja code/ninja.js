// Brevity is the soul of wit
// One-letter variables
// Use abbreviations
// Soar high. Be abstract.
// Attention test (use similar variable names)
// Smart synonyms
// Reuse names
// Underscores for fun
// Show your love (superElement, megaFrame and niceItem)
// Overlap outer variables
// Powerful functions! Don’t limit the function by what’s written in its name. Be broader.



i = i ? i < 0 ? Math.max(0, len + i) : i : 0; // ninja code - as short as possible

// more readable alternative
if (!i) {
    i = 0;
} else if (i < 0) {
    i = Math.max(0, len + i);
}
